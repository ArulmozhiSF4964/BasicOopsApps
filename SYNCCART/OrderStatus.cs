namespace SYNCCART
{
    /// <summary>
    /// OrderStatus is a enum contains value as Default, Ordered and Cancelled
    /// </summary> 

    public enum OrderStatus
    {
       Default, Ordered, Cancelled
    }
}